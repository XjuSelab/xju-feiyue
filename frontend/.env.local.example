# Copy to .env.local and uncomment to wire dev frontend to a real backend.
# When set, the dev mock dispatch table is skipped and all API calls go
# through fetch() to this base URL.
#
# VITE_API_BASE=http://localhost:8000
