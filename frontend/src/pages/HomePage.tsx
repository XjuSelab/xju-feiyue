/**
 * Round 3 placeholder. Round 4 home-agent 替换为完整页：
 *   WelcomeSection / CategoryGrid / LatestFeed / HotCarousel
 */
export function HomePage() {
  return (
    <section
      data-page="home"
      className="mx-auto max-w-5xl px-6 py-12"
    >
      <h1 className="font-serif text-3xl font-semibold text-text">
        LabNotes
      </h1>
      <p className="mt-2 text-sm text-text-muted">
        HomePage placeholder · Round 4 home-agent 接管。
      </p>
    </section>
  )
}
